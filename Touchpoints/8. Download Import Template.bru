meta {
  name: 8. Download Import Template
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/ops/api/v1/touchpoints/import/template
  body: none
  auth: inherit
}

headers {
  Business-Segment: TRADE
  Authorization: Bearer {{access_token}}
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

settings {
  encodeUrl: true
  timeout: 0
}
