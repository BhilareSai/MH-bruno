meta {
  name: Filters
  seq: 6
}

auth {
  mode: inherit
}

docs {
  Filter APIs for populating dropdowns.
  1. Static Filters — values from YAML config files (roles, LOBs, tiers, etc.)
  2. Geography Filters — paginated geography data from DB (zones, regions, depots, etc.)
}
